import React from "react";

function FilterProjects() {
  return (
    <div>
      FilterProjects
      <p>1. Add a project filter</p>
      <p>
        2. filter mockData through filter and then pass filtered data to
        projectList based on the filters
      </p>
      <p>3. In the project list, should display by default all mockData</p>
      <p>Have fun!</p>
    </div>
  );
}

export default FilterProjects;
